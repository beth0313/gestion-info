import numpy as np
a = np.array([1, 2, 3, 4, 5])
a = a + 2
#suma = np.sum(a)

matriz = np.ones((3, 3))
matriz = matriz + 4
suma = np.sum(matriz)
maximo = np.max(matriz)
#print(a)

print(matriz)
print(suma)
print(maximo)
#calcula el valor maximo de la raiz
#calciula la suma de todos los elementos